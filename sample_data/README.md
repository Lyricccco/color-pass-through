# Sample data

Place small, redistributable inference examples here. Processed RGB arrays must
be float `.npy` files in `H x W x 3` or `3 x H x W` order. DNG examples must
have suitable consent and redistribution rights.

The unit tests use generated tensors and do not depend on files in this
directory.
